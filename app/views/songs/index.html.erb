<h1>All songs</h1>

<% @songs.each do |song| %>
  <span class=song-title><%= song.title %></span>
  <span class=song-artist><%= song.artist.artist_name %></span>
  <span class=song-genre><%= song.genre.tryname %></span>
  <ul class=song-notes>
    <% song.notes.content :each do |note| %>
      <li class=song-note><%= note.content %></li>
    <% end %>
  </ul>

<% end %>
